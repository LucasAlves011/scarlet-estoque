spring:
  application:
    name: estoque
  servlet:
    multipart:
      max-request-size: 10MB
      max-file-size: 10MB
      enabled: true
  mvc:
    pathmatch:
      matching-strategy: ant-path-matcher
  profiles:
    active: lucas

